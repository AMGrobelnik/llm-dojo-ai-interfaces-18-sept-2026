# Python

## Runtime & packaging
- **uv** — Python package/dependency manager and virtualenv tool (research monorepo, notes repo)
- **ruff** — Python linter and formatter (research monorepo, notes repo)
- **ty** — static type checker for Python (research monorepo, notes repo)
- **deptry** — flags unused and missing Python dependencies (research monorepo)
- **import-linter** — enforces import/architecture boundaries (research monorepo)
- **pydantic-to-typescript** — generates TS types from Pydantic models (research monorepo)

## Data/ml
- **numpy** — numeric array computing (research monorepo, notes repo)
- **scipy** — scientific computing routines (notes repo)
- **pandas** — tabular data analysis (notes repo)
- **scikit-learn** — classical ML models and utilities (research monorepo, notes repo)
- **matplotlib** — plotting (research monorepo, notes repo)
- **seaborn** — statistical plotting on top of matplotlib (research monorepo)
- **lightgbm** — gradient-boosted tree models (notes repo)
- **autogluon.tabular** — automated tabular-data ML (notes repo)
- **torch** — deep learning framework (notes repo)
- **torchaudio** — audio utilities for torch (notes repo)
- **torchvision** — vision utilities for torch (notes repo)
- **lightning** — training loop framework for torch models (notes repo)
- **transformers** — pretrained transformer models (notes repo)
- **grad-cam** — CNN visual explanation (Grad-CAM) toolkit (notes repo)
- **librosa** — audio analysis (notes repo)
- **pyloudnorm** — audio loudness normalization (notes repo)
- **soundfile** — audio file I/O (notes repo)
- **demucs** — music source separation (notes repo)
- **faster-whisper** — fast speech-to-text inference (notes repo)
- **nltk** — natural-language processing toolkit (notes repo)
- **opencv-python** — computer vision toolkit (research monorepo, notes repo)
- **cca-zoo** — canonical correlation analysis models (notes repo)
- **umap-learn** — dimensionality reduction (notes repo)
- **datasets** — Hugging Face dataset loading (research monorepo)
- **huggingface-hub** — Hugging Face model/dataset hub client (research monorepo)
- **reverse-geocoder** — offline coordinate-to-place lookup (notes repo)

## Web/backend
- **fastapi** — async web API framework (research monorepo)
- **uvicorn** — ASGI server (research monorepo)
- **gunicorn** — WSGI application server (research monorepo)
- **django** — web framework for the backend service (research monorepo)
- **django-ninja** — typed API layer for Django (research monorepo)
- **django-cors-headers** — CORS handling for Django (research monorepo)
- **django-allauth** — authentication/OAuth for Django (research monorepo)
- **asgiref** — ASGI utility layer used by Django (research monorepo)
- **sqlalchemy** — SQL toolkit/ORM (research monorepo)
- **psycopg** — PostgreSQL driver (research monorepo)
- **dbos** — durable/orchestrated workflow execution engine (research monorepo)
- **websockets** — WebSocket client/server library (research monorepo)
- **aiohttp** — async HTTP client/server (research monorepo)

## Llm/api clients
- **openai** — OpenAI API client (research monorepo)
- **anthropic** — Anthropic API client (research monorepo)
- **google-genai** — Google Gemini API client (research monorepo)
- **litellm** — unified interface across multiple LLM providers (research monorepo)
- **openhands-sdk** — agentic coding-agent SDK (research monorepo)
- **openhands-tools** — tool implementations for the agentic SDK above (research monorepo)
- **tiktoken** — token counting for LLM inputs (research monorepo)

## Docs/office
- **python-pptx** — generates PowerPoint decks (notes repo)
- **pypdf** — PDF reading/manipulation (research monorepo)
- **PyMuPDF** — PDF parsing and rendering (research monorepo)
- **html2text** — HTML-to-Markdown conversion (research monorepo)
- **openpyxl** — reads/writes Excel spreadsheets (research monorepo)
- **beautifulsoup4** — HTML/XML parsing (research monorepo)
- **selectolax** — fast HTML parsing (research monorepo)

## Testing
- **pytest** — test runner (research monorepo, notes repo)
- **pytest-asyncio** — async test support for pytest (research monorepo)
- **pytest-xdist** — parallel test execution for pytest (research monorepo)

## Misc
- **pyyaml** — YAML parsing (research monorepo, notes repo)
- **loguru** — structured logging (research monorepo, notes repo)
- **pydantic** — data validation and settings models (research monorepo)
- **python-dotenv** — loads environment variables from `.env` files (research monorepo)
- **tenacity** — retry logic decorators (research monorepo)
- **jsonschema** — JSON Schema validation (research monorepo)
- **typing-extensions** — backported typing features (research monorepo)
- **pexpect** — drives interactive CLI processes (research monorepo)
- **requests** — HTTP client (research monorepo, notes repo)
- **httpx** — async-capable HTTP client (research monorepo)
- **cryptography** — cryptographic primitives (research monorepo, notes repo)
- **pycryptodome** — cryptographic primitives (notes repo)
- **threadpoolctl** — controls native thread pools in numeric libraries (notes repo)
- **nodriver** — undetected browser automation for autologin flows (research monorepo)
- **paramiko** — SSH client library (research monorepo)
- **rich** — terminal formatting and progress output (research monorepo)
- **tqdm** — progress bars (research monorepo)
- **lzstring** — string compression for URL-safe payloads (research monorepo)
- **psutil** — process and system utilities (research monorepo)
- **opentelemetry-api / -sdk** — distributed tracing instrumentation (research monorepo)
