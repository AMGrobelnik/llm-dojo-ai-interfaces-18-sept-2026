# Mining annex — open ONLY when community/speech source classes are in scope, or a P2-exit flag fires

API/reachability recipes rot, so they live ONLY here (never in cartography): every
reachability fact is `as_of`-stamped; append worked/403/429 outcomes per run so
regens skip dead paths. ALL hits here are **mining-lane leads** (SIFT + ship-state
rules cap them — uncorroborated speech/thread claims stay candidate).

## Dead zones (as_of 2026-06, re-verified live)
- Direct reddit.com → 403 from datacenter IPs (route: pullpush.io).
- YouTube caption pulls blocked from cloud IPs → use text mirrors first.
- Discord only via `site:answeroverflow.com` (zero hits = unindexed, never
  "no known issues").

## Verified quirks
- HN Algolia: keyless full-text over comments, but only `created_at_i`
  numericFilters work; documented `points`/`num_comments` filters → HTTP 400
  (sort client-side).
- Google Books anonymous quota pre-exhausted → key required.

## Routes (one line each)
- InfoQ: inline talk transcripts.
- Curated failure indexes (danluu/post-mortems, k8s.af) = free relevance filter.
- `<podcast:transcript>` RSS tag (Tapesearch/Podscan fallback).
- Book snippet oracles (Google Books volumes API · Manning liveBook ·
  Archive.org search-inside) — verification, not discovery.
- SO errata: comments and newer higher-voted answers outrank the accepted
  answer for deprecation/gotcha claims.

## P2-exit counter-query recipes (one per red flag)
- Domain-share flag → orthogonal-vocabulary probe outside the dominant domains.
- Recency flag → `before:2023` vs last-12-months time-slice diff → supersession rows.
- URL:origin flag → second-retrieval-class cross-check.
